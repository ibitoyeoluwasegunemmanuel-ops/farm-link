{
  "buildCommand": "NODE_OPTIONS=--max-old-space-size=4096 ./node_modules/.bin/expo export --platform web",
  "outputDirectory": "dist",
  "framework": null,
  "installCommand": "npm install --legacy-peer-deps",
  "rewrites": [
    { "source": "/(.*)", "destination": "/index.html" }
  ]
}
